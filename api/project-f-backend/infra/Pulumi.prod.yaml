config:
  aws:region: ap-southeast-1
  project-f-infra:queueName: optimize-job
  # sqsEndpoint intentionally absent → real AWS SQS
  # sshPublicKey must be set as a secret before first deploy:
  #   pulumi config set --secret sshPublicKey "$(ssh-keygen -y -f your-key.pem)" --stack prod
